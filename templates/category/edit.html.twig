{% extends 'admin/base.html.twig' %}

{% block body %}
<div class="main">
<div class="form">
<h1>Edit Category</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}<br>
    <button type="submit" class="btn btn-primary">Update</button>
    {{ form_end(form) }}
    <br>
    <br>

</div>
{% endblock %}